// Array: 

// pop() : remove element from end.
// shift(): remove element from starting.

let arr = ["orange", "apple", "Mango", "Grapes"];
arr.pop();
console.log(arr);
arr.shift();
console.log(arr);

// some: if some element in array are present according to condition. Print: true/false.
const num = [1,2,3,4,5,6];

const  
// every: if every element in array are present according to condition. Print: true/false. 

